import {renderComponent, expect } from '../test_helper';
import CommentBox from '../../src/CommentBox';

describe("Comment box tests", () => {
	it("should contain a text area" , () => {
		const commentBoxRendered = renderComponent(CommentBox);
		
	});

	it("should have a button", () => {

	});

	it("should clear the text area when button is pressed", () => {

	});
})